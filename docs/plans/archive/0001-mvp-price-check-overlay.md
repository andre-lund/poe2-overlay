---
status: done
created: 2026-06-23
updated: 2026-07-12
completed: 2026-07-12
adrs: [1, 2, 3, 4, 5, 6, 7]
commit refs: [53331f7 (T1), b9b7869 (T2), 9a3ee30 (T3), cd60a03 (T4), 4bb78c0 (T5), d729d50 (T6), 0b1fcf1 (T7), e93f0dd (T8), ff9c633 (T9), 9abc639 (T10), 0c27bff (T10), c654e11 (T10), eb8c8c6 (T11), c2b0ff1 (T11)]
---

# Plan: MVP price-check overlay (then map/atlas + regex)

## Intent

Deliver a working PoE2 trade overlay on KDE Wayland: press a hotkey over an
in-game item and see live pricing in an overlay drawn on top of the game. This
is the floor that makes the project useful; the map/atlas danger-checker and
regex helpers build on the same overlay + input + data foundation afterward.

## Approach

Per [ADR-0001](../adr/0001-clean-build-rust-tauri-layer-shell-overlay.md): a Rust
+ Tauri 2 + Vue app whose GTK window is promoted to a `wlr-layer-shell` OVERLAY
surface; input via `evdev` (hotkey) + `uinput` (synthesized copy) + clipboard;
pricing via poe.ninja (bulk) and the GGG trade2 API (gear). The Tauri shell is
scaffolded; module seams exist in `src-tauri/src/{overlay,hotkey,trade}.rs`.
Reference implementations: ExileWatch (Rust/gtk-layer-shell), PathofTrading
(Quickshell, validated on this machine), Waystone (portal GlobalShortcuts) —
read, do not copy.

The make-or-break unknown is whether KWin composites the layer-shell OVERLAY
surface over *exclusive* fullscreen; T2 must test both fullscreen modes and
record the result (borderless is the known-good fallback).

## Tasks

- [x] **T1** — Build prereqs + confirm the empty Tauri shell runs. **Done:** all Tauri Linux build deps already present (webkit2gtk-4.1, gtk3, base-devel, openssl, librsvg, libappindicator-gtk3) — no sudo install needed; `npm install` + `npm run build` pass; `cargo build` clean (4 expected stub warnings); `npm run tauri:dev` launches the hidden window (vite :1420, `Running target/debug/poe2-overlay`, no panic — only a benign `libx265` GStreamer plugin-scan warning). Added `tauri:dev`/`tauri:build` scripts setting `GDK_BACKEND=wayland` + `WEBKIT_DISABLE_DMABUF_RENDERER=1`.
- [x] **T2** — Layer-shell overlay surface: add `gtk-layer-shell`/`gdk`, promote the main window (Layer::Overlay, anchors, on-demand keyboard), show/hide. **Verify over a real Proton PoE2 game.** **Impl:** activated `gtk = "0.18"` + `gtk-layer-shell = { "0.8", features = ["v0_6"] }` (matches Tauri 2.11's gtk-rs 0.18; `v0_6` gates `KeyboardMode`). `overlay::init_layer_shell` promotes the still-hidden GTK window — `Layer::Overlay`, **all-four-edge anchors (full-output surface)**, `exclusive_zone(-1)`, `KeyboardMode::OnDemand`, namespace `poe2-overlay`. The card is positioned top-right via CSS within the full-screen canvas; `hide_overlay` command unmaps it; ✕ button + Esc dismiss. **Make-or-break CONFIRMED:** the full-output overlay composites over fullscreen Proton PoE2 — the user saw the popup drawn on top of the running game. **Two corrected dead-ends:** (1) full-output + `set_ignore_cursor_events` click-through trapped *all* input — tao sets the input-shape on the *toplevel* GDK window, which the WebKitGTK child surface ignores, so the transparent screen ate clicks with no exit (user-reported); fix = a ✕/Esc-dismissable modal (PathofTrading's full-screen, focusable, show-on-demand model) rather than click-through. (2) a corner-sized surface (top+right, 2 anchors) collapses to ~0 size — gtk-layer-shell takes the size from the WebKitGTK child whose min-size request is ~0, so nothing renders (user saw nothing); **full-output (4 anchors) is required** to force a non-zero size. (`Exclusive` keyboard focus also rejected — it drops the game out of fullscreen.) Compiles clean (only T3/T4 stub warnings); surface maps (instrumented `map-event` + `size-allocate`). **Tooling caveat:** `spectacle`/KWin screencast does not reliably capture the transparent overlay layer over a fullscreen game on this HDR setup (captured it once on a clean desktop, not since) — visual confirmation is by eye, not screenshot. **Done (user-confirmed):** the card shows top-right composited over fullscreen PoE2 and ✕/Esc dismisses. While shown it is modal (covers the screen); T3 hides it by default + shows on the hotkey.
- [x] **T3** — Input path: global hotkey → `uinput` Ctrl+C synth → X11 clipboard read. **Pivoted to a KDE global shortcut (ADR-0002), not evdev:** evdev can read keys but not *consume* them, so a chord on a game-bound key (PoE2's `D`) leaks to the game and moves the character (confirmed: Alt+D moved the character). A KWin global shortcut intercepts the chord first — the proven PathofTrading mechanism. **Impl (code-complete; in-game test pending):** `Ctrl+Alt+D` is a KDE service shortcut running `poe2-overlay --price-check`; `tauri-plugin-single-instance` (first plugin) forwards it to the running app → `hotkey::price_check` synthesizes Ctrl+C via a warm `uinput` device (`Synth` in Tauri state) → 120 ms → reads the X11 CLIPBOARD selection (`x11-clipboard` crate, no `xclip` binary) → emits `price-check-item` + shows the overlay; ✕/Esc hides. No `/dev/input` reads, so **no `input` group / sudo** — only the `/dev/uinput` session ACL. Verified end-to-end minus the game: second-instance forwarding fires `price_check`, synth device builds, clipboard read returns text (27 chars seeded). Took over PathofTrading's `Ctrl+Alt+D` (kglobalshortcutsrc + a `poe2-overlay-pricecheck.desktop`; backup saved). **Input-trap incident + fix ([ADR-0003]):** the first in-game trigger locked the machine out — `window.show()` mapped the **full-output** T2 surface, whose `wl_surface` input region (CSS `pointer-events:none` does not shrink it) swallowed every click with only an on-screen ✕/Esc to escape; hard-restart required. Fixed by making the surface a **fixed-size sub-output rectangle** (`set_size_request` **+ `resize(1,1)`** — tao pre-pins the window to the conf size 1140×600, so the `resize` is what commits the real size; corrects T2's "full-output required", which was just a missing size request) so it can never cover the screen, plus a compositor-level **`Ctrl+Alt+X` → `--hide`** shortcut (single-instance forwarding, ADR-0002) as a guaranteed escape. Adversarially audited — no residual lockout. **Then three functional bugs, fixed in order:** (1) **repeat checks read an empty clipboard** — *not* the focus theory first chased (KeyboardMode/GTK focus props made no difference); the real cause is an **XWayland clipboard read-race** — the game's copy reaches the X11 selection only after KWin's sync, and a single read at 120 ms catches a transient-empty mid-sync state. Fixed by **polling** the clipboard (~40 ms × up to 20, ~800 ms) until non-empty, matching PathofTrading's retrying backend. (2) **stacked ghost popups** — a content-sized card shrinks for shorter items and WebKitGTK leaves the previously-painted transparent region uncleared until a later repaint. Fixed with a **fixed-size 400×380 card** that overpaints the same region every time, plus a `show()`-only-when-hidden guard. (3) **panel moved to screen-centre** (per user; the corner was too far to glance at) — surface unanchored+centred 440×420. `KeyboardMode::None` kept on its own merit (a game overlay must not steal the keyboard from PoE2). **Done (user-confirmed in-game):** hover item + Ctrl+Alt+D shows the centred price card; repeat checks replace it cleanly; ✕/Ctrl+Alt+X dismiss; no lockout. **Follow-ups (T6):** `Ctrl+Alt+X` needs the live `kglobalacceld` to reload (relogin) to fire; and a first launch with no running instance drops its own `--price-check`/`--hide` flag (the single-instance callback fires only for the 2nd instance) and leaves a stray hidden process holding `/dev/uinput` — fold into autostart.
- [x] **T4** — Pricing core: parse PoE2 item text; bulk via poe.ninja, gear via GGG trade2 search+fetch with `X-Rate-Limit` handling; persistent warm HTTP client. **Done (code-complete; in-game test pending):** New `trade/` module — a faithful Rust reimplementation of PathofTrading's `backend.py` (GPLv3, technique reference only, [ADR-0004]): `parse.rs` (item-text parser, bulk-vs-gear, stats w/ affix tier+source), `stats.rs` (`StatMapper` fuzzy stat-text→trade2 id w/ pseudo totals + reduced→increased / less→more / synonym / chance-to fallbacks; `base_name` base-type resolution), `ninja.rs` (poe.ninja bulk + exchange rates), `gear.rs` (trade2 query build → search+fetch → exalt-normalized sorted listings), `mod.rs` (warm async `reqwest` client + daily-stale trade2 `data/stats`/`data/items`/league caches + 15-min exchange-rate cache, held in Tauri state; IP `X-Rate-Limit` lockout; live league resolution). Async pricing is driven from the hotkey worker thread via `tauri::async_runtime::block_on` (the thread is a plain `std::thread`, not a runtime worker, so this is safe). `hotkey::price_check` now parses → emits `price-check-loading` → prices → emits `price-check-result` (two-phase contract); `App.vue` renders it minimally (rich per-stat-toggle / league-selector / requery UI is T5). **Two stale-reference bugs caught by live API probing + fixed:** (1) the reference's hardcoded league "Fate of the Vaal" is dead — the current league is "Runes of Aldur", and a stale league in the trade2 search path returns HTTP 400 "Invalid query"; we now resolve the active league from the live league list (poe.ninja is lenient about the league param, which masked this on the bulk path). (2) the reference's `NINJA_CURRENCY_MAP` is dead code — poe.ninja keys high-value orbs by short ids (`divine`, not `divine-orb`) and returns null names, so common currency silently fell through to the GGG auction; we wired the map in so currency prices on the zero-quota poe.ninja path. **Verified end-to-end against live APIs** (ignored smoke tests, `cargo test -- --ignored`): bulk Divine→"1 D", Chaos→"38.51 E"; a rare body-armour gear search → Success, 10 listings, pseudo total-life + pseudo total-elemental-resistance filters correctly mapped (75% res ×0.8 = min 60; 89 life ×0.8 = min 71). `cargo build` + `clippy` clean; 11 offline unit tests pass. **Adversarial multi-agent review (5 dimensions, each finding independently verified) → 4 confirmed, all fixed:** (high) a 429 never armed GGG's `Retry-After` penalty into the lockout — only the ~1 s `window/limit` estimate — so a mashed hotkey could re-fire into an active penalty and escalate toward an IP ban → now arm the lockout from `Retry-After` on every 429, also read the active-restriction header field, and added an `IN_FLIGHT` guard so re-entrant triggers are dropped; (med) Normal-rarity base type (the "Superior " quality prefix) wasn't resolved via the item-type list → widened the deferred resolution to all non-Unique/Rare items; (med) std-mutex poisoning + a `from_secs_f64` overflow on a hostile rate-limit header could panic/brick pricing → poison-recovering locks + finite-checked, hour-clamped, `checked_add` lockout arming; (low) the truncated-Rare fallback now matches the reference's `lines[0]`. **Follow-up:** confirm in-game that a hovered rare/currency shows live prices, and that the parser's `Level:`→gem-level rule does not misfire on real PoE2 gear requirement lines (assumed "Requires: Level N", which is metadata-skipped — verify).
- [x] **T5** — Overlay UI (Vue): listings, per-stat filter toggles, requery, league selector; transparent/click-through styling. **Done (code-complete; in-game test pending):** Implements the ADR-0004 contract end-to-end. **Backend:** extracted `gear::run_gear_query` from `price_gear` (runs the trade2 search+fetch from *pre-built* filters, so requery reuses it with user-edited ones); `Pricing` gained `last_item` (the last `ParsedItem`, so requery re-prices without the frontend round-tripping the whole item), `set_league(Option<String>)` override, and `requery(league, parsed_stats, base_properties)` (bulk re-priced via poe.ninja for the new league, gear via `run_gear_query` with the edited filters, IP-lockout-gated); `ParsedStat`/`BaseProp` gained `Deserialize` to round-trip from JS; the rates cache now keys on `rates_league` so a league switch refetches+replaces (was a single unkeyed map → stale on switch). An async `#[tauri::command] requery` (in `lib.rs`) bridges the overlay to it. **Frontend (`App.vue`):** league `<select>` (requeries on change), toggleable base-property + stat-filter rows (checkbox + editable min/max), an explicit **Requery** button (not auto-requery-per-keystroke — that would risk the GGG IP limit), loading-vs-busy states, polished listings. Surface enlarged to 470×640 (`overlay.rs`) for the richer panel; the card stays a constant-size internally-scrolling region (the T3/ADR-0003 ghost-repaint fix). **Verified:** `cargo build` + `clippy` clean; 11 offline unit tests; frontend `vue-tsc` type-check; **3 live smoke tests pass** (bulk Divine→"1 D"/Chaos→"38.82 E"; rare gear search→Success/10 listings; **requery with edited filters→Success/10**). **Adversarial multi-agent review (4 dimensions, each verified) → 4 confirmed, all fixed:** (high) the requery lockout pre-check emptied `parsed_stats`/`base_properties`, which `applyResult` blindly repopulates from → wiped the user's filter edits; now threads the edited filters back (matching the reference); (med) a failed exchange-rate refetch on league switch silently served the *previous* league's rates as the new league's prices → falls back to neutral seeds + forces a retry; (med) filter checkboxes/min-max stayed editable during an in-flight requery so edits got clobbered on resolve → `:disabled="busy"`; (low) a hotkey check fired mid-requery could be overwritten by the stale requery result → a `reqGen` generation token drops the stale result. **Follow-up:** in-game visual pass (toggles + league selector + requery against a real hovered item; the transparent layer-shell surface can't be screenshotted headless).
- [x] **T6** — Package + launch: AppImage build, KDE global shortcut + uinput setup docs, autostart. **Done (artifacts built + installer dry-run-validated; on-machine install + in-game pass pending):** `npm run tauri:build` produces **AppImage + deb + rpm** under `src-tauri/target/release/bundle/` (baked `APPIMAGE_EXTRACT_AND_RUN=1` into the `tauri:build` script so the AppImage bundles even without FUSE — needed in this sandbox; a normal desktop with FUSE doesn't). `packaging/install.sh` (user-level, idempotent): installs the bare release binary to `~/.local/bin/poe2-overlay` (preferred over the 104 MB AppImage for a local install — instant spawn, system WebKit; AppImage is the portable artifact), writes the two flag-forwarder launchers (`--price-check`/`--hide`) + an **autostart** entry (the warm instance, no flag), registers the `Ctrl+Alt+D`/`Ctrl+Alt+X` KWin shortcuts via `kwriteconfig6`, installs the hicolor icon, and checks `/dev/uinput` (printing — never running — the sudo udev step). `packaging/99-poe2-overlay-uinput.rules` is a `uaccess` udev rule for machines lacking the session ACL. **Folds in both T3 follow-ups:** autostart provides the warm instance so the first `Ctrl+Alt+D` forwards to it (no stray idle process dropping its own flag), and the docs make the `kglobalacceld` relogin requirement explicit. Docs: new `docs/INSTALL.md` runbook (build → install → uinput → relogin → use → troubleshoot → uninstall); README status updated scaffold → "price-check MVP working (T1–T6)". **Verified:** `tauri:build` produced all three bundles; `install.sh` passed a scratch-`HOME` dry-run (correct binary, launchers, autostart, icon, and `kglobalshortcutsrc` `[services][…desktop] _launch=` entries matching the proven T3 form) on both the binary-preferred and AppImage-fallback paths. **Not run against the real KDE config** (changes autostart + live shortcuts — left to the user). **Follow-up:** run `./packaging/install.sh`, relogin, and do the in-game pass (price-check + the T5 UI against a real hovered item; and confirm the parser's `Level:`→gem-level rule doesn't misfire on real gear requirement lines).
- [x] **T7** — (post-MVP) Map/atlas danger-checker: flag dangerous waystone/map mod combinations. **Done (code-complete; in-game test + ruleset refinement pending):** New top-level `danger` module ([ADR-0005]): `analyze(&ParsedItem) → DangerReport` (level Safe/Caution/Dangerous/Deadly + matched flags w/ label, "why", and the matched mod line), driven by a static, build-agnostic keyword ruleset. `hotkey::price_check` routes a **Waystone** (item class `Waystones`) to the danger-checker and emits a new `price-check-danger` event — **not priced** (local, instant, zero GGG quota); non-waystones price as before. `App.vue` renders a severity-colored danger panel that swaps cleanly with the price panel. **Ruleset grounded in the real PoE2 mod surface** (mined from the live trade2 `data/stats`, not PoE1 priors): the grounding pass confirmed PoE2 waystones have **none** of the PoE1 reflect / no-regen / -max-res / extra-as-element(PoE1-phrasing) mods, and surfaced the real lethal surface — "Monsters deal % of Damage as Extra Chaos" (Deadly; bypasses ES), Extra Fire/Cold/Lightning, monster crit chance/damage, +Attack/Cast/Movement Speed, additional projectiles, "Monsters deal increased Damage", reduced recovery, Elemental Weakness curse, Mark for Death, Delirium, "deal no damage 3/10s" (Dangerous); Temporal Chains/Enfeeble curses, reduced flask charges, poison/bleed on hit, charge steal, armour break, accuracy, AoE, grasping vine, flammability, shock/freeze/stun (Caution) — with beneficial mods ("Monsters take increased Damage", "Players have more Recovery") correctly NOT flagged. Two combos escalate to Deadly (crit-chance × crit-damage; reduced-recovery × amplified-damage). **Verified:** 18 offline unit tests (7 danger) incl. clean=Safe, extra-chaos=Deadly, crit combo, severity tiers; `cargo build`/`clippy` clean; frontend `vue-tsc` type-check. **Adversarial review (2 dims) → 3 confirmed (2 distinct), fixed:** (med) the `has_extra_element` combo detector matched the "Extra projectiles" label by prefix, false-escalating reduced-recovery + extra-projectiles to Deadly → tightened to `starts_with("Extra ") && ends_with(" damage")` + 2 regression tests; (low) the `price-check-danger` listener left `busy` stuck true when it interrupted an in-flight requery → reset `busy` in the danger (and, symmetrically, loading) listener. **Follow-up:** in-game pass on a real waystone (confirm the copied mod text matches the keywords — clipboard wording ≈ trade text but verify) and refine the ruleset/severities to taste (it's plain data).
- [x] **T8** — (post-MVP) Regex helpers: stash/vendor search regex builder + cheat-sheets. **Done (code-complete; in-game test + pattern refinement pending):** Shipped as a **pointer-only cheat-sheet** ([ADR-0005]→ no, [ADR-0006]): a curated library of PoE2 stash/vendor search-regex patterns; click a pattern → the app **writes the X11 clipboard** → you paste (Ctrl+V) into the in-game Ctrl-F box. Grounded by a research workflow that confirmed (GGG forum + community, PoE2-specific) that PoE2's stash+vendor search *does* support regex (alternation, classes/ranges, `.`, quantifiers, anchors, `!` exclusion, space-AND) matching the item's full text incl. mods. **Backend:** `cheatsheet.rs` (static const pattern table → `get_cheatsheet` command, ~14 patterns across Defenses/Damage/Rarity/Waystone, `SEARCH_CHAR_LIMIT`=250 surfaced; `rarity:`/tier patterns carry `verify` notes); `clipboard.rs` (`Clip(Mutex<Clipboard>)` in state — the app's first X11 *write*; `store()` is non-blocking and the warm instance's background thread serves the game's paste as a fellow XWayland X11 client — the write-side mirror of `hotkey::read_x11_clipboard`); `copy_to_clipboard` command. **Trigger:** a new KDE shortcut `Ctrl+Alt+F` → `--regex` → single-instance emits `show-regex` + shows the panel (not folded onto price-check — no item context); the installer adds a 3rd `.desktop` + `kglobalshortcutsrc` entry (`POE2_REGEX_KEY` override). **Frontend:** a cheat-sheet panel (categorized click-to-copy rows + "✓ Copied" flash + char-limit footer) as a third mutually-exclusive overlay mode (price / danger / cheat-sheet). **Decision (with the user):** pure cheat-sheet, NOT an interactive builder — the overlay never takes keyboard focus (ADR-0003) so a typed editor is impossible, the value is the correct patterns (authored data like `danger.rs`), and a composer is deferred to v2; clipboard-only (no auto-paste — can't verify the in-game box is focused). **Verified:** 19 offline unit tests (cheat-sheet char-limit/non-empty), `cargo build`/`clippy` clean, frontend `vue-tsc` type-check. **Adversarial review (2 dims) → 1 confirmed (low), fixed:** the `show-regex` handler cleared the price/danger panels *before* the `get_cheatsheet` await, flashing the stale price card for one IPC frame → now fetches first and swaps atomically (prior panel holds, no flash); the X11 write model, serde↔TS contract, `--regex` wiring, and installer shortcut were all spot-checked clean. **Follow-up:** in-game — confirm the X11 clipboard write pastes into the game under Proton, and refine/verify the pattern set + `rarity:`/tier syntax on the current patch (plain data in `cheatsheet.rs`).

- [x] **T9** — (post-MVP) Rune price sheet on `Ctrl+Alt+F`: poe.ninja rune prices for reward panels the game can't copy from. **Done (code-complete + live-triggered; in-game eyeball pending):** Motivated by the Runeshape Combinations reward panel — the user confirmed manual Ctrl+C copies **nothing** from its tooltips (no clipboard support on reward UIs), so hover-copy price checks are impossible there; the Overwolf apps that price rewards read the screen instead. Chosen design (over a typed name-search box and an OCR hover path, both deferred): a deliberate hotkey shows **every rune poe.ninja tracks for the active league, most valuable first, with a client-side filter box** — one poe.ninja round-trip, zero GGG quota, works while the game is paused. **Backend:** `ninja.rs` gains `fetch_rune_sheet` (Runes overview, per-category `primaryValue` is exalt-denominated per the T4 finding; name derived from the dash-id since poe.ninja returns null names — apostrophes are lost, "Craiceanns" not "Craiceann's") + a shared `display_value` E/D formatter extracted from `price_bulk`; `Pricing::rune_sheet` resolves the league via the usual cache snapshot; `get_rune_sheet` command + a `--runes` single-instance branch (emit `show-runes` + show, the old `--regex` pattern). **Frontend:** a fourth mutually-exclusive panel (price / danger / cheat-sheet / runes) with the same fetch-first anti-flash swap as T8's; the filter strips apostrophes on both sides so typing the in-game "Craiceann's" still hits. **Trigger:** reuses the `Ctrl+Alt+F` key freed by the disabled T8 regex sheet — installer writes `poe2-overlay-runes.desktop` + the `kglobalshortcutsrc` entry (`POE2_RUNES_KEY` override); regex remnants still cleaned up. **Verified:** 23 offline unit tests (name_from_id title-casing), `cargo test` + `tauri build` clean, both Craiceann runes confirmed present in the live Runes overview (144 entries), and `--runes` fired against the warm instance without error. **Follow-up:** in-game eyeball of the sheet + Ctrl+Alt+F after kglobalacceld reload (relogin, or the restarted `plasma-kglobalaccel.service` this session).

- [x] **T10** — (post-MVP) Generalize the T9 rune sheet into a **category price sheet**: `Ctrl+Alt+F` still opens on Runes, but the panel now has Rust-owned category tabs (Runes / Currency / Fragments / Essences / Omens / Soul Cores / Abyss / Breach / Delirium / Expedition / Idols / Uncut Gems / Lineage Gems — Abyssal Bones live under Abyss) switching between poe.ninja overview types with one round-trip per switch, zero GGG quota. **Backend:** `fetch_rune_sheet` → `fetch_price_sheet(ninja_type)` + `SHEET_CATEGORIES` (label → overview type; first entry = hotkey default); `RuneSheet`/`RuneEntry` → `PriceSheet` (adds `category` + `categories` so the UI's tabs stay Rust-owned) / `SheetEntry`; `get_rune_sheet` → `get_price_sheet(category)` (unknown/empty label falls back to Runes). **Currency quirk handled:** unlike the other per-category overviews (already exalt-denominated per the T4 finding), the Currency overview is denominated against exalted's own value — normalized by dividing by the `exalted` line (same as `fetch_exchange_rates`), and its short ids (`divine`, `alch`) get real names via the reverse of `CURRENCY_MAP`. **Frontend:** category chip row on the sheet panel (active-highlighted, disabled + list dimmed `stale` while a switch is in flight); filter resets per category; reopening via the hotkey always lands on Runes. **Verified:** 25 offline unit tests (`sheet_name` currency-alias reversal added), `cargo test` + `vue-tsc` clean; all 13 category types probed live (Currency 52 / Runes 145 / … / Abyss 15 lines incl. collarbones/jawbones/ribs) and the Currency normalization confirmed against live values (chaos 0.1272 ÷ exalted 0.001531 ≈ 83 E). **Follow-up:** in-game eyeball of the tabs + eyeball a couple of Currency prices against the in-game exchange. **Extended same-session — grouped catalogue + item overviews + collapsible tabs:** categories now live under three groups — **General** (the 13 exchange categories), **Equipment** (Unique Weapons / Armours / Accessories / Jewels / Flasks), **Atlas** (Precursor Tablets / Unique Tablets) — via `SHEET_GROUPS` (group → `SheetSource::Exchange|Items` → categories); Equipment/Atlas use the newly discovered **item overview** endpoint (`poe2/api/economy/stash/current/item/overview`, found by watching the poe.ninja site's network traffic — the exchange endpoint returns 0 lines for these types). Item overviews are divine-denominated with `core.rates.exalted` as the exalt converter (cached divine rate as fallback) and return real names; variant lines are disambiguated in the row label (`item_entry_name`: base type when ≠ name for uniques on two bases, `detailsId` rarity suffix for tablets' normal/magic/rare split — normal unmarked, plus `[corrupted]`). Payload's flat `categories` → `groups: Vec<SheetGroupInfo>`; UI gets group tabs (underline style) over the category chips + a **▾/▸ categories collapse toggle** in the header line to reclaim card space. Verified: 25 offline tests (`item_entry_name` variant tagging added), live probes of all 7 item types (UniqueWeapons 148 / UniqueArmours 440 / … / PrecursorTablets 21 / UniqueTablets 9 lines), `vue-tsc` + CLI build clean, deployed to the warm instance. **Denomination correction (user-caught: Aldur's Saga showed 46.71 E vs poe.ninja's 47 D):** the T4-era "per-category exchange `primaryValue` is exalt-denominated" assumption is wrong — *every* overview in this API (exchange and item alike) denominates `primaryValue` in the league's primary display unit (divine today) and ships its own converter in `core.rates.exalted`; the sheet's exchange fetch and `price_bulk`'s category probe (which silently under-priced essences/runes/omens ~658× whenever they missed the Currency cache) now both convert via `Overview::exalts_per_primary` (cached divine rate as fallback), the sheet's Currency special-case scale is gone (subsumed), and real names (apostrophes intact) come from the response's previously-ignored top-level `items` metadata. `fetch_exchange_rates` was always correct (its divide-by-exalted-line is denomination-agnostic). Expedition (logbooks/sagas/fluxes) also moved General → Atlas, turning the per-group endpoint into a per-category `SheetSource`. Note: the two gear-path live smoke tests currently fail with GGG "Invalid search (check base type)" on HEAD too — pre-existing stale fixture, tracked separately.

- [x] **T11** — (post-MVP) PoE2 retheme + stability/UX fixes from a full-codebase review. **Done (user-confirmed in-game 2026-07-12):** **Retheme (`App.vue`):** the card now borrows PoE2's item-tooltip vocabulary instead of the modern blue/Inter look — near-black warm panel with the bronze double-edge frame (squared 3px corners, gilt inner bevel), centered item name in **rarity colors** (rare yellow / unique orange / magic blue / currency, derived from the echoed `rarity` base-property), gilt gradient separator under the header, bronze gradient buttons + category chips, gold-on-black listings with the cheapest price rendered one step larger, styled scrollbar, reduced-motion-safe pulse on "Searching market…", and an empty state that teaches all three hotkeys with `kbd` chips. Type is **Fontin** (+ Fontin SmallCaps) — PoE's own typeface; its license forbids redistribution so it is NOT committed: `packaging/install.sh` gained a best-effort fetch from exljbris into `~/.local/share/fonts/fontin/` (installed on this machine directly) and the CSS stack degrades to system serifs. Theme documented in `DESIGN.md` (+ `PRODUCT.md`); INSTALL.md notes the font step. **Stability/UX fixes:** (1) *stale-panel race* — the `show-runes`/`show-regex` listeners and `switchCategory` fetched, then assigned unconditionally, so a price check fired during the fetch got buried under the stale sheet when it resolved; all three now guard on the existing `reqGen` token like `requery`. (2) *Typed filters were dead in-game* — `KeyboardMode::None` (ADR-0003) never granted keyboard focus, so the T5 min/max bounds and T9 sheet filter could not receive keystrokes; **[ADR-0007] supersedes ADR-0003** with `KeyboardMode::OnDemand` (focus only after a click on the panel; sized surface + `Ctrl+Alt+X` compositor escape carried forward unchanged; revert to `None` is the one-line fallback if fullscreen Proton misbehaves on focus loss). (3) *Same-item re-check showed "No item"* — the clipboard-change guard can never fire when the game copies identical text; `hotkey::price_check` now re-prices an unchanged item-shaped clipboard after the ~1.5 s poll (trade-off documented in-code: pressing over nothing with an old item text on the clipboard re-prices that old item, self-explanatory since the card names it). (4) *Truncated-Rare garbage name* — the `parse.rs` fallback used `lines[0]` (the "Item Class:" header, reference-faithful but user-visible garbage); now falls back to the name line (test updated). (5) *Rate-limit countdown* — the static "wait Ns" card now ticks down (frontend `rateWait` interval parsed from the result message; requery button + league select gated while counting, flips to "Rate limit cleared" at 0; the backend lockout stays the source of truth). Also: ✕ tooltip no longer claims Esc (inert under `None` when written; live again when focused under OnDemand), "Requery" → "Search again". **Verified:** `vue-tsc` + `vite build` clean; 25 offline Rust tests pass (truncated-rare test updated); `cargo clippy` clean; theme eyeballed via a rendered 4-state preview (price+filters / danger / sheet / empty) over a bright mock scene. **In-game verification (user, 2026-07-12):** theme pass over the real game ("much better"); typed filters work under OnDemand focus — fullscreen Proton survives the panel click (ADR-0007's gate cleared, no fallback to `None` needed); T11 verified in full. Commits: eb8c8c6 (fixes + retheme), c2b0ff1 (deeper tooltip look).

**Plan closed 2026-07-12:** T4–T10 user-confirmed in-game (pricing + danger against
real items, price-sheet visual pass) on top of the earlier T1–T3 and T11
confirmations — every task verified, plan archived.

## Decision log

- Scope split: T1-T6 = price-check MVP; T7-T8 (map/atlas + regex) are in project
  scope but sequenced after the MVP works end-to-end.
- gtk-layer-shell/evdev/reqwest are declared (commented) in `Cargo.toml` and
  activated per task to keep the scaffold buildable before system libs are present.
- T2 overlay model: corner-sized surface (top+right, 460×160), hidden-by-default,
  shown on demand, dismissable — matching the proven PathofTrading reference. NOT a
  full-output click-through canvas: `set_ignore_cursor_events` sets the input-shape on
  the toplevel GDK window, which the WebKitGTK child surface ignores, so a full-output
  surface eats every click. Confirmed compositing over fullscreen PoE2 (user saw the
  corner popup over the running game), so the make-or-break is settled for this stack.
- T2 fallback (if a future fullscreen mode ever hides it): borderless/Windowed
  Fullscreen is the PathofTrading-validated config; exclusive fullscreen could in
  principle trigger a KWin direct-scanout bypass. Diagnostic lever if so:
  `KWIN_DRM_NO_DIRECT_SCANOUT=1` in the game env, or System Settings → Display &
  Monitor → Compositor "Allow applications to block compositing".
- T2 tooling note: `spectacle`/KWin screencast does not reliably capture a transparent
  layer-shell surface composited over fullscreen (returns a transparent frame). Verify
  the overlay by eye, not by screenshot.
- T5 will decide the real panel: a larger sized surface still has transparent dead
  zones that catch clicks within its bounds; per-region input (or content-sized
  surface) is the T5 seam — not needed for the corner probe.
- T3 input mechanism pivoted evdev → KDE global shortcut ([ADR-0002]). evdev can't
  consume keys, so a game-bound chord (PoE2 `D`) leaks and moves the character. The
  KDE shortcut consumes the chord and removes the `input`-group requirement. `xclip`
  (ADR-0001's pick) isn't installed; we read the X11 selection in-process via the
  `x11-clipboard` crate — no external binary, same X11 selection.
- T3 copy is plain Ctrl+C (PoE2 basic copy, confirmed by PathofTrading's
  run_pricecheck.sh). Advanced item text (Ctrl+Alt+C) is a one-line synth change
  once basic pricing works.
- **Overlay surface model: full-output → sized, centred, focus-free ([ADR-0003],
  supersedes the T2 full-output decision above).** The full-output surface trapped all
  screen input (CSS `pointer-events:none` does not shrink a `wl_surface` input region)
  and locked the user out → hard restart. The surface is now a fixed-size unanchored
  (centred) rectangle, sized with the gtk-layer-shell two-call idiom
  (`set_size_request(440,420)` **then** `resize(1,1)` — tao pre-pins the window to the
  conf size 1140×600 and `set_size_request` only raises the minimum, so the `resize`
  is what commits the real size; T2's "collapse-to-0" was simply a missing size
  request). It cannot cover the screen. Plus **`KeyboardMode::None`** (a game overlay
  must not steal the keyboard from PoE2) and a compositor-level `Ctrl+Alt+X` → `--hide`
  escape. Per-region click-through *within* the panel stays the T5 seam.
- **Repeat-checks empty clipboard = XWayland read-race, NOT focus (dead-end recorded so
  it is not re-chased).** First theory was focus theft (overlay grabs focus → clipboard
  bridge empties); `KeyboardMode::None`, hide-before-copy, and GTK `accept_focus`/
  `focus_on_map(false)` ALL failed to change the symptom. Real cause: the game's copy
  reaches the X11 CLIPBOARD only after KWin's XWayland sync, and a single read at 120 ms
  catches a transient-empty state (proven by the diagnostic that the item appeared on
  the *next* press's pre-read). Fix: **poll** the clipboard (~40 ms × up to 20) until
  non-empty — the same retry PathofTrading's backend does. The speculative focus props
  were removed in the cleanup; `KeyboardMode::None` stays for the keyboard reason above.
- **Stacked ghost popups = WebKit transparent-repaint, fixed by a constant-size card.**
  A content-sized card shrinks for shorter items; WebKitGTK does not clear the
  previously-painted transparent region until a later repaint, so old cards linger
  stacked (they clear "after some time"). Fix: a **fixed 400×380 card** that overpaints
  the same region each update, plus `show()` only when the window is hidden (calling it
  on an already-mapped layer surface was a suspected second cause; the fixed size was
  the actual fix).

- **T4 pricing architecture is [ADR-0004]:** warm async `reqwest` client + daily-stale
  trade2 reference caches in Tauri state; async pricing driven from the hotkey worker
  thread via `block_on`; a two-phase `price-check-loading` → `price-check-result` event
  contract (the `PriceResult` shape T5 builds its toggles/requery on); IP rate-limit
  lockout gating every trade2 search/fetch (poe.ninja bulk ungated). Recorded as an ADR
  because the event contract + IP-ban policy outlive T4.
- **The `backend.py` reference has two stale/dead spots we deliberately diverge from
  (verified against the live 2026 APIs):** its hardcoded league is out of date, so we
  resolve the active league from the fetched league list (GGG trade2 rejects a stale
  league path → 400); and its `NINJA_CURRENCY_MAP` is defined but never wired in, so we
  use it (poe.ninja keys orbs by short ids + returns null names) to keep common currency
  on the zero-quota poe.ninja path per ADR-0001.
- **poe.ninja denomination (confirmed empirically):** the Currency exchange overview is
  divine-based (`divine`=1), normalized to exalt-equivalents in `fetch_exchange_rates`;
  the per-category overviews (Essences/Runes/Omens/…) are already exalt-denominated, so
  their `primaryValue` is used directly — the reference's `÷ rates["exalt"]` was a no-op,
  removed for clarity, not a behavior change.
- **Stat-filter fidelity, with one deliberate divergence from the reference:** most stats
  map to pseudo aggregates (total life/mana/ES/attributes), and the seed bound is 80% of
  the rolled value (full value for Grants Skill / Bonded / Adds; negative rolls bound the
  `max`). Pseudo ids confirmed present in PoE2's `data/stats`. **Resistances stay
  per-element** (fire/cold/lightning each → their own `pseudo_total_*_resistance`) rather
  than the reference's sum-into-one-`total-elemental-resistance` — the aggregate hid the
  item's actual mods (a lightning+cold item showed "+X% total Elemental Resistance") and
  searched a far broader, cheaper pool, so the shown price was a near-worthless lower
  bound. Per-element filters match items genuinely like the copied one → accurate display
  and price. "to all Elemental Resistances" still maps to its own all-elemental pseudo.
- **Rate-limit lockout hardened past the reference (review finding):** the reference (and
  our first cut) only self-throttled `window/limit` (~1 s) on a 429 and never armed GGG's
  real `Retry-After` penalty — fine per-keypress where the human throttles, dangerous in a
  persistent app a hotkey can hammer. We arm the lockout from `Retry-After` on every 429,
  read the active-restriction header field, clamp to 1 h, never shorten a live lockout, and
  drop re-entrant checks with an `IN_FLIGHT` guard. This is the IP-ban path ADR-0004 exists
  to prevent.
- **T4 verifies by ignored network smoke tests, not in-game:** `cargo test -- --ignored`
  hits poe.ninja (bulk) and one trade2 search+fetch (gear) — enough to confirm the live API
  shapes + the full pipeline without the game. In-game confirmation (a real hovered item) is
  the T4 follow-up folded into T5/T6 testing.

- **T5 requery keeps item identity server-side.** Rather than serialize the whole item
  to JS and back, `Pricing` stores the last `ParsedItem` and the requery command carries
  only the edited filters + league; the backend re-prices the stored item. Matches the
  reference's `--requery-data` intent without the round-trip. `run_gear_query` is the
  shared core so initial-check and requery build the identical query.
- **Requery is an explicit button, not auto-per-keystroke; league change requeries
  directly.** Auto-requerying on every toggle/min-max edit would fire a GGG search per
  keystroke and risk the IP limit (ADR-0004). Toggles stage locally; one button (and a
  league change, a single deliberate action) fires the search. Controls are disabled
  while a requery is in flight.
- **Exchange rates are keyed by league (`rates_league`).** T4 cached a single unkeyed
  rate map; with the T5 league selector that served stale cross-league prices on a
  switch. Rates now refetch+replace when the league changes, and a *failed* refetch for a
  new league drops to neutral seeds (never the prior league's ratios) and retries next
  check.
- **T5 surface is 470×640, card fills it with internal scroll.** Settles the "T5 will
  decide the real panel" seam: a constant-size card (the T3/ADR-0003 ghost-repaint fix)
  large enough for the league selector + filter toggles + listings, bounded so clicks
  outside still reach the game.
- **No new ADR for T5** — it implements the ADR-0004 backend↔overlay contract; the local
  build decisions live here.
- **T6 installs the bare release binary, not the AppImage, for a local install.** The
  AppImage (the T6 "AppImage build" deliverable) is built and is the portable/distribution
  artifact, but for the user's own machine the 19 MB binary spawns instantly (the AppImage
  adds mount/AppRun latency to every flag-forwarder) and links the system WebKit we built
  against. Installer prefers the binary, falls back to the AppImage.
- **Warmth comes from autostart, not the shortcut (T6 settles the T3 follow-ups).** The
  KDE shortcuts only forward a flag; `tauri-plugin-single-instance` needs a running
  instance to forward to, so the autostart `.desktop` (no flag) is what makes the first
  `Ctrl+Alt+D` work — and avoids the stray idle process. `kglobalacceld` loads
  `kglobalshortcutsrc` only at session start, so install requires a relogin (documented,
  not worked around).
- **No T6 ADR** — packaging/launch are implementation of the existing decisions; build +
  install specifics live here and in `docs/INSTALL.md`.
- **T7 danger-checker is [ADR-0005]:** a price-check on a Waystone yields a danger verdict
  (separate `price-check-danger` event + `DangerReport`), not a price; the trigger folds
  into the existing hotkey, and the ruleset is a static, build-agnostic keyword table.
  Recorded as an ADR because "waystones show danger, not price" + the new event contract
  outlive T7, and to pin the grounding finding so reflect/no-regen aren't re-added.
- **The danger ruleset is grounded in live PoE2 data, not PoE1 memory.** Mining the trade2
  `data/stats` before writing the rules caught that the PoE1 staples (reflect, no-regen,
  -max-res) don't exist in PoE2 waystones and surfaced the real lethal mods (extra-Chaos,
  Mark for Death, Delirium, "deal no damage 3/10s", charge-steal). Keyword matching ignores
  the rolled numbers, so it's robust to the value-substituted clipboard text.
- **Build-awareness (the rejected T7 option) is deferred.** The static ruleset is the v1; a
  per-build danger profile would tailor severities and, if built, supersede ADR-0005.
- **T8 regex helpers are [ADR-0006]:** a pointer-only cheat-sheet (not a builder — the overlay
  can't take keyboard focus, ADR-0003), copied via the app's first X11 clipboard *write*, opened
  by its own `Ctrl+Alt+F` shortcut. Grounded by research confirming PoE2 search *does* support
  regex (over stash+vendor, full-text incl. mods). Clipboard-only (no auto-paste).
- **T9 rune sheet: reward-panel pricing goes name-based via poe.ninja, not OCR; `Ctrl+Alt+F` is
  rebound from the dormant regex sheet.** The game offers no clipboard copy on reward tooltips
  (user-verified on the Runeshape panel), so the hover-copy pipeline can never price them. Of the
  three name-based options (rune sheet / typed search box / OCR hover parity), the sheet is the
  smallest diff answering the actual question ("which reward is worth more") — it reuses the T8
  card pattern and the T4 ninja path, and a search box or OCR can layer on later if list-scanning
  chafes. Rebinding the freed `Ctrl+Alt+F` avoids burning a new chord; no new ADR — ADR-0006's
  *decision* (regex via clipboard-write cheat-sheet) is untouched, only its dormant trigger key is
  reused, and everything else here implements existing ADR-0004 infrastructure.
- **All eight tasks are code-complete; in-game verification is the remaining gate before the plan
  archives.** T1–T3 were user-confirmed in-game; T4–T8 are code-complete + reviewed but await the
  in-game pass (pricing/danger/regex against real items + the X11-write paste under Proton). The
  plan stays `active` until that pass; map/atlas + regex were the planned post-MVP scope and are
  now built.

[ADR-0002]: ../adr/0002-kde-global-shortcut-hotkey.md
[ADR-0003]: ../adr/0003-overlay-dismissal-safety-corner-surface-hide-shortcut.md
[ADR-0004]: ../adr/0004-pricing-core-warm-client-event-contract.md
[ADR-0005]: ../adr/0005-waystone-danger-check-on-price-check.md
[ADR-0006]: ../adr/0006-regex-cheatsheet-clipboard-write.md
