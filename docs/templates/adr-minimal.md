---
status: proposed
---

# NNNN — <decision title: what was decided, not the topic>

**Implemented by:** _pending_
<!-- A single line, always present. When an implementing plan exists, replace
     `_pending_` with a link to it AND the implementing commits (grouped by
     task id), e.g.
       **Implemented by:** [docs/plans/active/<slug>.md](../plans/active/<slug>.md), commits abc1234 (T1) · def5678 (T2)
     then repoint the link to ../plans/archive/<slug>.md in the same commit that
     archives the plan. The commits mirror the plan's `commit refs:` frontmatter;
     all other as-built detail lives in the PLAN, never here. -->
<!-- If this ADR SUPERSEDES a prior one, add a `**Supersedes:** ADR-NNNN` line
     under the title here, and flip the superseded ADR's frontmatter `status:`
     to `superseded by ADR-NNNN`. -->

<One-paragraph statement of the decision, in the present tense — what is now
true for the repo.>

## Context

<The forces in 2–4 sentences: the problem, the constraints, the prior
decisions that shape this. If `status: rejected`, the reason lives here so the
option is not re-litigated.>

## Decision

<The decision and its immediate rationale. **This body is immutable once the
frontmatter `status:` is `accepted`** — a change is a NEW ADR that supersedes
this one (flip this ADR's `status:` to `superseded by ADR-NNNN`). Never edit an
accepted decision in place; never append dated amendments.>

## Consequences

<What becomes true, what is now harder, what is explicitly deferred.>
